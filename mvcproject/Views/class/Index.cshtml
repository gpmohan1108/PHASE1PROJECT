@model IEnumerable<studentmanagent.Models.classmodel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.class_roomno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.class_strength)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.class_roomno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.class_strength)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.class_roomno  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.class_roomno }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.class_roomno })
        </td>
    </tr>
}

</table>
